/// Named routes used by [App]'s router.
class AppRoutes {
  AppRoutes._();

  static const String login = '/';
  static const String prestart = '/prestart';
  static const String welcome = '/welcome';
}
